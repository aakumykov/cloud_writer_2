package com.github.aakumykov.cloud_writer

import org.junit.Assert
import org.junit.Test

class VirtualRootPlusUnitTests {

    @Test
    fun `соединяет два пути в один`() {
        Assert.assertEquals("","")
    }
}